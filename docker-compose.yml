version: "3"
services:
	web:
		image: ogalaemmanuel/devops-assignment:guestbook
		deploy:
			replicas: 5
			resources:
				limits:
					cpus: "0.1"
					memory: 50M
			restart_policy:
				condition: on-failure
		ports:
			- "80:3000"
		networks:
			- webnet
	networks:
		webnet:
			
